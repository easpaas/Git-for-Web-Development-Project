Link to codepen for Part Two:
  https://codepen.io/easpaas/pen/gOppNLJ?editors=1100 
 
Q/A:

    1. What is Semantic HTML? 
      Semantic provides meaning and allows for readability for machines and humans.

    2. What is HTML used for? 
      HTML is used to provide structure. 

    3. What is an attribute and where do we put it? 
      An attribute is always placed in the opening tag and provides more info about a tag.

    4. What is the h1 tag used for? How many times should I use it on a page?
      An h1 tag is used for page titles. Should only be used once per page. 

    5. Name two tags that have required attributes?
      Two tags that have required attributes are <input> and <select>.

    6. What do we put in the head of our HTML document? 
      Metadata

    7. What is an id? 
      Specific reference to a tag with the id attribute. Used for CSS. 

    8. What elements can I add an id to? 
      Any HTML element can have an id. They must be different though, as ids must be unique. 

    9. How many times can I use the same id on a page? 
      Only one time. 

    10. What is a class? 
      A class attribute specifies more than one classname for HTML elements. 
    
    11. What elements can I add a class to? 
      Any element can have a class attribute. 

    12. How many times can I use the same class on a page? 
      Class can be used as many times as needed on a page. 

    13. How do I get my link to open in a new tab?
      <a href="url" target="_blank">

    14. What is the alt attribute used for? 
      The alt attribute provides text as an alternative to the image. Helpful for visually impaired. 

    15. How do I reference an id?
      To reference an id use a hastag followed by the id name.

    16. What is the difference between a section and a div
      A section tag defines sections in a document. A div groups a block of elements together. 

    17. What is CSS used for? 
      CSS provides styling to the HTML elements. 

    18. How to we select an element? Example - every h2 on the page
      h2 {
        
      }

    19. What is the difference between a class and an id? - Give me an example of when I might use each one
      Use of a class to apply the same style to multiple elements on a page. Use an id to apply style to a unique element. 
    
    20. How do we select classes in CSS?
      To select classes use a period followed by the class name. 

    21. How do we select a p element with a single class of “human””?
      p.human {

      }

    22. What is a parent child selector? When would this be useful? 
      A parent child selector is a reference to a child element. It becomes useful when you want to target a specific child element. 

    23. How do you select all links within a div with the class of sidebar?
      div.sidebar{

      }

    24. What is a pseudo selector?
      A pseudo selector is used to define a special state of an element. 

    25. What do we use the change the spacing between lines?
      To change line spacing use line-height.

    26. What do we use to change the spacing between letters?
      To change spacing between letters use letter-spacing.

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
      Text-transform. 

    28. How do I add a 1px border around my div that is dotted and black?
      Use the shorthand property - border:  1px dotted black;

    29. How do I select everything on the page? 
      * {

      }

    30. How do I write a comment in CSS?
      /* CSS comment here */

    31. How do I find out what file I am in, when I am using the command line? 
      pwd command

    32. Using the command line - how do I see a list of files/folders in my current folder?
      ls command

    33. How do I remove a file via the command line? Why do I have to be careful with this? 
      rm (filename) 

    34. Why should I use version control? 
      Version control allows multiple collaborators on the same project. Also gives a project a digital timeline. 

    35. How often should I commit to github?
      At least every 20 minutes, especially if there are working changes.

    36. What is the command we would use to push our repo up to github?
      git push 

    37. Walk me through Lambda's git flow. 
      Fork respository. Add TL as collaborator. Clone project url to file on local machine. Create a branch with 'firstname-lastname'. 
      Add, commit, and push new branch back up to repository. Pull request with TL as a reviewer when there have been new additions. 

Stretch Questions

    1. What is the difference between an inline element and a block element?
      Inline elements do not force a new line to begin. Whereas, block elements typically cause a line break to occur.

    2. What happens when an element is positioned absolutely? 
      The element position value is absolute. 
      "This value creates a new stacking context when the value of z-index is not auto. 
      The margins of absolutely positioned boxes do not collapse with other margins." - Referenced from https://developer.mozilla.org/en-US/docs/Web/CSS/position

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
      Add a width property to the element. 

    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
      Display block by default:
        - <p>
        - <section>
        - <address>

      Display inline by default: 
        - <a>
        - <br>

      Display inline-block by default: 
        - <img>  
    
    5. In your own words, explain the box model. What is the fix for the box model? 
      The box model explains how HTML elements are modeled. 
      The fix for the box model is box-sizing: border-box.
